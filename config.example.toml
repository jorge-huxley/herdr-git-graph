# Optional configuration for herdr-git-graph.
# Copy to the directory printed by: herdr plugin config-dir herdr-git-graph

# Maximum commits to load into the graph (50–5000).
# commit_limit = 500

# External diff highlighter command (defaults to `delta` when found on PATH).
# delta_command = "delta"
